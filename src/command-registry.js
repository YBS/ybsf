const COMMANDS = [
  {
    name: "convert-config",
    description: "Convert legacy config files to ybsf-metadata-config.json",
    usage: "ybsf convert-config [--input-dir <path>] [--output-dir <path>] [--target-org <alias>] [--api-version <version>] [--force] [--includeManagedPackages] [--includeUnlockedPackages] [--debug]",
    flags: [
      "--input-dir",
      "--output-dir",
      "--target-org",
      "--api-version",
      "--force",
      "--includeManagedPackages",
      "--includeUnlockedPackages",
      "--debug",
    ],
    shortFlags: ["-i", "-d", "-o", "-a", "-f"],
    flagDetails: [
      { long: "--input-dir <path>", short: "-i", description: "Legacy config directory to read from. Default: current directory." },
      { long: "--output-dir <path>", short: "-d", description: "Directory to write ybsf-metadata-config.json into. Default: current directory." },
      { long: "--target-org <alias>", short: "-o", description: "Optional org alias for org-aware conversion behavior." },
      { long: "--api-version <version>", short: "-a", description: `Metadata API version. Default: repo constant.` },
      { long: "--force", short: "-f", description: "Overwrite generated output when needed." },
      { long: "--includeManagedPackages", description: "Include managed package metadata in generated config rules." },
      { long: "--includeUnlockedPackages", description: "Include unlocked package metadata in generated config rules." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
  },
  {
    name: "init-project",
    description: "Initialize SFDX project structure and optionally convert legacy config",
    usage: "ybsf init-project [--api-version <version>] [--target-org <alias>] [--force] [--includeManagedPackages] [--includeUnlockedPackages] [--debug]",
    flags: [
      "--api-version",
      "--target-org",
      "--force",
      "--includeManagedPackages",
      "--includeUnlockedPackages",
      "--debug",
    ],
    shortFlags: ["-a", "-o", "-f"],
    flagDetails: [
      { long: "--api-version <version>", short: "-a", description: "Metadata API version to place into scaffolded config/project files." },
      { long: "--target-org <alias>", short: "-o", description: "Optional org alias for org-aware conversion/normalization." },
      { long: "--force", short: "-f", description: "Allow overwriting scaffolded files when appropriate." },
      { long: "--includeManagedPackages", description: "Include managed package metadata in generated config rules." },
      { long: "--includeUnlockedPackages", description: "Include unlocked package metadata in generated config rules." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
  },
  {
    name: "generate-manifest",
    description: "Generate manifest/package.xml from JSON config",
    usage: "ybsf generate-manifest [--config <path>] --target-org <alias> [--output <path>] [--debug]",
    flags: ["--config", "--output", "--target-org", "--debug"],
    shortFlags: ["-c", "-p", "-o"],
    flagDetails: [
      { long: "--config <path>", short: "-c", description: "Config file to read. Default: ybsf-metadata-config.json." },
      { long: "--output <path>", short: "-p", description: "Manifest output path. Default: manifest/package.xml." },
      { long: "--target-org <alias>", short: "-o", description: "Target org alias used for metadata discovery." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
  },
  {
    name: "normalize-config",
    description: "Normalize ybsf-metadata-config.json and optionally reconcile to org discovery",
    usage: "ybsf normalize-config [--config <path>] [--target-org <alias>] [--init-mode] [--includeManagedPackages] [--includeUnlockedPackages] [--debug]",
    flags: [
      "--config",
      "--target-org",
      "--init-mode",
      "--includeManagedPackages",
      "--includeUnlockedPackages",
      "--debug",
    ],
    shortFlags: ["-c", "-o", "-i"],
    flagDetails: [
      { long: "--config <path>", short: "-c", description: "Config file to normalize. Default: ybsf-metadata-config.json." },
      { long: "--target-org <alias>", short: "-o", description: "Optional org alias for discovery-backed reconciliation." },
      { long: "--init-mode", short: "-i", description: "Seed newly discovered metadata types as enabled defaults." },
      { long: "--includeManagedPackages", description: "Set packageRules.includeManagedPackages=true during normalization." },
      { long: "--includeUnlockedPackages", description: "Set packageRules.includeUnlockedPackages=true during normalization." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
  },
  {
    name: "retrieve",
    description: "Generate manifest, retrieve metadata, and run post-retrieve transforms",
    usage: "ybsf retrieve --target-org <alias> [--debug]",
    flags: ["--target-org", "--debug"],
    shortFlags: ["-o"],
    flagDetails: [
      { long: "--target-org <alias>", short: "-o", description: "Target org alias to retrieve metadata from." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
  },
  {
    name: "destructive-preview",
    description: "Generate destructive candidate summary from org vs desired manifest",
    usage: "ybsf destructive-preview [--config <path>] --target-org <alias> [--debug]",
    flags: ["--config", "--target-org", "--debug"],
    shortFlags: ["-c", "-o"],
    flagDetails: [
      { long: "--config <path>", short: "-c", description: "Config file to read. Default: ybsf-metadata-config.json." },
      { long: "--target-org <alias>", short: "-o", description: "Target org alias used to compute destructive candidates." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
  },
  {
    name: "validate-deploy",
    description: "Run check-only deploy from manifest/package.xml with optional destructive apply",
    usage: "ybsf validate-deploy [--config <path>] --target-org <alias> [--apply-destructive] [--test-level <level>] [--tests <comma-separated-tests>] [--debug]",
    flags: ["--config", "--target-org", "--apply-destructive", "--test-level", "--tests", "--debug"],
    shortFlags: ["-c", "-o", "-l", "-t"],
    flagDetails: [
      { long: "--config <path>", short: "-c", description: "Config file to read. Default: ybsf-metadata-config.json." },
      { long: "--target-org <alias>", short: "-o", description: "Target org alias to validate against." },
      { long: "--apply-destructive", description: "Apply generated destructiveChanges.xml without the follow-up prompt." },
      { long: "--test-level <level>", short: "-l", description: "Deploy test level. Example: RunLocalTests or RunSpecifiedTests." },
      { long: "--tests <comma-separated-tests>", short: "-t", description: "Required with --test-level RunSpecifiedTests. Comma-separated Apex test names." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
  },
  {
    name: "deploy",
    description: "Run deploy from manifest/package.xml with optional destructive apply",
    usage: "ybsf deploy [--config <path>] --target-org <alias> [--apply-destructive] [--test-level <level>] [--tests <comma-separated-tests>] [--debug]",
    flags: ["--config", "--target-org", "--apply-destructive", "--test-level", "--tests", "--debug"],
    shortFlags: ["-c", "-o", "-l", "-t"],
    flagDetails: [
      { long: "--config <path>", short: "-c", description: "Config file to read. Default: ybsf-metadata-config.json." },
      { long: "--target-org <alias>", short: "-o", description: "Target org alias to deploy to." },
      { long: "--apply-destructive", description: "Apply generated destructiveChanges.xml without the follow-up prompt." },
      { long: "--test-level <level>", short: "-l", description: "Deploy test level. Example: RunLocalTests or RunSpecifiedTests." },
      { long: "--tests <comma-separated-tests>", short: "-t", description: "Required with --test-level RunSpecifiedTests. Comma-separated Apex test names." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
  },
  {
    name: "document",
    description: "Generate metadata documentation CSV files",
    usage: "ybsf document <task> [--object <ObjectApiName> | --all] [--source-dir <path>] [--output-dir <path>] [--target-org <alias>] [--debug]",
    flags: ["--object", "--all", "--source-dir", "--output-dir", "--target-org", "--debug"],
    shortFlags: ["-a", "-s", "-d", "-o"],
    flagDetails: [
      { long: "--object <ObjectApiName>", description: "Process one object. Required unless --all is used." },
      { long: "--all", short: "-a", description: "Process all available objects. Mutually exclusive with --object." },
      { long: "--source-dir <path>", short: "-s", description: "Metadata source directory. Default: force-app/main/default." },
      { long: "--output-dir <path>", short: "-d", description: "CSV output directory. Default: doc." },
      { long: "--target-org <alias>", short: "-o", description: "Required for objectFields. Optional for local metadata tasks." },
      { long: "--debug", description: "Keep run artifacts under tmp/ for inspection." },
    ],
    notes: [
      "Tasks: objectFields, picklistValues, picklistValuesControllingField, picklistValuesRecordTypes",
    ],
  },
  {
    name: "completion",
    description: "Print shell completion script",
    usage: "ybsf completion [zsh]",
    flags: [],
    shortFlags: [],
    notes: ["Supported shell: zsh"],
  },
  {
    name: "version",
    description: "Print CLI version",
    usage: "ybsf --version",
    flags: [],
    shortFlags: [],
  },
  {
    name: "help",
    description: "Show help",
    usage: "ybsf help [command]",
    flags: [],
    shortFlags: [],
    notes: ["Also supported: ybsf <command> --help"],
  },
];

function getCommand(name) {
  return COMMANDS.find((c) => c.name === name) || null;
}

module.exports = {
  COMMANDS,
  getCommand,
};
